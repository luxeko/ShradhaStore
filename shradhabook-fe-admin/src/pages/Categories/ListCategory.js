import React from 'react';

const ListCategory = () => {
    return (
        <div className={`list-category`}>
            this is category page
        </div>
    );
};

export default ListCategory;